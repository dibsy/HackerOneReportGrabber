Install the dependencies and add to path the both pip install scripts and wkhtmltopdf

If pip is not installed then install pip , then issue this command

pip install beautifulsoup4
pip install json2html

